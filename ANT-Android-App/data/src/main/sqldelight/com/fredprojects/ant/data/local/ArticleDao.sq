upsertArticle:
INSERT OR REPLACE
INTO ArticleEntity(title, description, date, articleType, content)
VALUES ?;

getAllArticles:
SELECT * FROM ArticleEntity;

deleteArticle:
DELETE FROM ArticleEntity WHERE id = :id;